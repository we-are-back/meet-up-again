import { getCenter } from "geolib";
// import checkPostcodes from "./postcodesApiRequest";

const input = checkPostcodes.valid

const getCoordinates = (latitude, longitude) => {

}

let input1 = {latitude: , longitude: }
let input2 = {latitude: , longitude: }
let inputArr = [input1, input2]

geolib.getCenter(inputArr);